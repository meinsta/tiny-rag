qdrant-client>=1.9.0
requests>=2.31.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
# Optional: install for hybrid (dense + sparse BM42) search.
# Without it the demo falls back to dense-only retrieval.
fastembed>=0.4.0
